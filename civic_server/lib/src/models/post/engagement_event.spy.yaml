class: EngagementEvent
table: engagement_event
fields:
  user: UserRecord?, relation(onDelete=Cascade)
  post: Post?, relation(onDelete=Cascade)
  type: String
  createdAt: DateTime?, default=now
indexes:
  engagement_event_user_post_type_idx:
    fields: userId, postId, type
    unique: false
