class: PostBookmarks
table: post_bookmarks
fields:
    post: Post?, relation(onDelete=Cascade)
    owner: UserRecord?, relation
    dateCreated: DateTime, default=now
    

indexes:
  post_bookmarks_id_unique_idx:
    fields: postId, ownerId
    unique: true