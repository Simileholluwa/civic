class: PostReposts
table: post_reposts
fields:
    post: Post?, relation(onDelete=Cascade)
    parent: Post?, relation(onDelete=Cascade)
    owner: UserRecord?, relation
    dateCreated: DateTime?, default=now

indexes:
  post_reposts_id_unique_idx:
    fields: postId, ownerId, parentId
    unique: true