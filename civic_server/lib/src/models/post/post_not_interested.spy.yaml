class: PostNotInterested
table: post_not_interested
fields:
  user: UserRecord?, relation
  post: Post?, relation
  dateMarked: DateTime?, default=now
indexes:
    post_not_interested_id_unique_idx:
        fields: userId, postId
        unique: true
