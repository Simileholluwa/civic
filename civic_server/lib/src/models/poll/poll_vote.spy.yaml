class: PollVote
table: poll_vote

fields: 
    poll: Poll?, relation(onDelete=cascade)
    option: PollOption?, relation(onDelete=cascade)
    voter: UserRecord?, relation
    votedAt: DateTime?, default=now
    

indexes:
  poll_vote_unique_idx:
    fields: pollId, voterId
    unique: true