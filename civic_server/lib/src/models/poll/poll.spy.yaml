class: Poll
table: poll

fields: 
    post: Post?, relation(onDelete=Cascade, optional)
    owner: UserRecord?, relation
    options: List<PollOption>?, relation
    expiresAt: DateTime?
    votedBy: List<int>?
    