class: Poll
table: poll

fields:
    owner: UserRecord?, relation(onDelete=Cascade)
    options: List<PollOption>?, relation(name=poll_options)
    expiresAt: DateTime?
    votesCount: int?, default=0
    