class: PollLikes
table: poll_likes
fields:
    poll: Poll?, relation(onDelete=Cascade)
    owner: UserRecord?, relation
    dateCreated: DateTime?

indexes:
  poll_id_unique_idx:
    fields: pollId, ownerId
    unique: true