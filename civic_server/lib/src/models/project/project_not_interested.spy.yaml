class: ProjectNotInterested
table: project_not_interested
fields:
  user: UserRecord?, relation(onDelete=Cascade)
  project: Project?, relation(onDelete=Cascade)
  dateMarked: DateTime?, default=now
indexes:
    project_not_interested_id_unique_idx:
        fields: userId, projectId
        unique: true
