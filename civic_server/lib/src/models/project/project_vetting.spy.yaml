class: ProjectVetting
table: project_vetting
fields:
    project: Project?, relation
    owner: UserRecord?, relation
    images: List<String>?
    comment: String?
    status: String?
    createdAt: DateTime?, default=now
    updatedAt: DateTime?
    