class: ProjectBookmarks
table: project_bookmarks
fields:
    project: Project?, relation(onDelete=Cascade)
    owner: UserRecord?, relation(onDelete=Cascade)
    dateCreated: DateTime, default=now

indexes:
  project_bookmarks_id_unique_idx:
    fields: projectId, ownerId
    unique: true