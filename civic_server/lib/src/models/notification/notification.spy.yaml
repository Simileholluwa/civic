class: Notification
table: notification
fields:
  receiver: UserRecord?, relation
  sender: UserRecord?, relation
  groupedSenderIds: List<int>?
  content: String?
  groupKey: String?
  actionType: String
  targetType: String
  actionRoute: String
  targetId: int
  isRead: bool, default=false
  createdAt: DateTime, default=now
  updatedAt: DateTime?
